---
title: "try-catch-finally"
weight: 2
---

## Overview

The `try-catch-finally` structure is how Java handles exceptions. Code that might fail goes in `try`, the response to a specific failure goes in `catch`, and cleanup code that always runs goes in `finally`.

> Notes coming soon. Check back as content is added throughout the term.
