---
title: "break and continue"
weight: 3
---

## Overview

`break` and `continue` give you fine-grained control inside a loop. `break` exits the loop entirely; `continue` skips the rest of the current iteration and moves to the next one.

> Notes coming soon. Check back as content is added throughout the term.
