---
title: "if and if-else"
weight: 1
---

## Overview

The `if` statement is the most basic decision-making tool in Java. Combined with `else` and `else if`, you can handle multiple conditions and control exactly which code runs.

> Notes coming soon. Check back as content is added throughout the term.
