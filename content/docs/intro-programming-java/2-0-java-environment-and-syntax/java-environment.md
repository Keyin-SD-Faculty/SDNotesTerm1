---
title: "The Java Environment"
weight: 1
---

## Overview

Java programs don't run directly on your hardware — they run inside the Java Virtual Machine (JVM). Understanding the relationship between the JDK, JRE, and JVM is key to knowing what's happening when you compile and run code.

> Notes coming soon. Check back as content is added throughout the term.
